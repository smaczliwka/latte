int main() {
    // l1
    int x = readInt();
    if (x > 0) {
        // l2
        return 17;
    }
    else {
        // l4
        while (true) { // l5 - warunek
            // l6
            int y = readInt();
            if (y < 8)
                return 0; // l7
            // l9
        }
        // l10
    }
    int a = 2, b = 3;
    int c = a + b; // 5
    int c2 = a + b;
    int d = a - b; // -1
    int d2 = a - b;
    int e = a * b; // 6
    int e2 = a * b;
    int f = b / a; // 1
    int f2 = b / a;

    printInt(c + c2 + d + d2 + e + e2 + f + f2);
    return 0;
}